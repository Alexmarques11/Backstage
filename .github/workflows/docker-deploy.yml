name: Docker Hub Deploy

on:
  push:
    branches: [ main ]
    paths: 
      - 'backend/**'
      - '.github/workflows/docker-deploy.yml'
  release:
    types: [ published ]

env:
  REGISTRY: docker.io
  # Current Docker Hub user configuration
  # SERVER_IMAGE: ${{ secrets.DOCKER_USERNAME }}/backstage-server
  # AUTH_IMAGE: ${{ secrets.DOCKER_USERNAME }}/backstage-auth
  
  # Alternative Docker Hub users (uncomment to switch):
  # For Goncalo's account:
  SERVER_IMAGE: ${{ secrets.DOCKER_USERNAME_GONCALO }}/backstage-server
  AUTH_IMAGE: ${{ secrets.DOCKER_USERNAME_GONCALO }}/backstage-auth

  # For ZE's account:
  # SERVER_IMAGE: ${{ secrets.DOCKER_USERNAME_ZE }}/backstage-server
  # AUTH_IMAGE: ${{ secrets.DOCKER_USERNAME_ZE }}/backstage-auth

jobs:
  build-and-push-server:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        # Current Docker Hub credentials
        #username: ${{ secrets.DOCKER_USERNAME }}
        #password: ${{ secrets.DOCKER_PASSWORD }}
        
        # Alternative Docker Hub credentials (uncomment to switch):
        # For Goncalo's account:
        username: ${{ secrets.DOCKER_USERNAME_GONCALO }}
        password: ${{ secrets.DOCKER_PASSWORD_GONCALO }}
        
        # For ZE's account:
        # username: ${{ secrets.DOCKER_USERNAME_ZE }}
        # password: ${{ secrets.DOCKER_PASSWORD_ZE }}

    - name: Extract metadata for Server
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.SERVER_IMAGE }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push Server Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile.server
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha,scope=server
        cache-to: type=gha,mode=max,scope=server

    - name: Server image pushed successfully
      run: |
        echo "✅ Server Docker image pushed successfully!"
        echo "🐳 Docker Hub: https://hub.docker.com/r/${{ secrets.DOCKER_USERNAME }}/backstage-server"
        echo "📦 Pull command: docker pull ${{ secrets.DOCKER_USERNAME }}/backstage-server:latest"

  build-and-push-auth:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        # Current Docker Hub credentials
        # username: ${{ secrets.DOCKER_USERNAME }}
        # password: ${{ secrets.DOCKER_PASSWORD }}
        
        # Alternative Docker Hub credentials (uncomment to switch):
        # For Goncalo's account:
        username: ${{ secrets.DOCKER_USERNAME_GONCALO }}
        password: ${{ secrets.DOCKER_PASSWORD_GONCALO }}
        
        # For ZE's account:
        # username: ${{ secrets.DOCKER_USERNAME_ZE }}
        # password: ${{ secrets.DOCKER_PASSWORD_ZE }}

    - name: Extract metadata for Auth Server
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.AUTH_IMAGE }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push Auth Server Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile.auth
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha,scope=auth
        cache-to: type=gha,mode=max,scope=auth

    - name: Auth Server image pushed successfully
      run: |
        echo "✅ Auth Server Docker image pushed successfully!"
        echo "� Docker Hub: https://hub.docker.com/r/${{ secrets.DOCKER_USERNAME }}/backstage-auth"
        echo "�📦 Pull command: docker pull ${{ secrets.DOCKER_USERNAME }}/backstage-auth:latest"

  test-docker-images:
    runs-on: ubuntu-latest
    needs: [build-and-push-server, build-and-push-auth]
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        # Current Docker Hub credentials
        # username: ${{ secrets.DOCKER_USERNAME }}
        # password: ${{ secrets.DOCKER_PASSWORD }}
        
        # Alternative Docker Hub credentials (uncomment to switch):
        # For Goncalo's account:
        username: ${{ secrets.DOCKER_USERNAME_GONCALO }}
        password: ${{ secrets.DOCKER_PASSWORD_GONCALO }}

        # For ZE's account:
        # username: ${{ secrets.DOCKER_USERNAME_ZE }}
        # password: ${{ secrets.DOCKER_PASSWORD_ZE }}

    - name: Test Server Docker image from Docker Hub
      run: |
        echo "Testing Server image: ${{ secrets.DOCKER_USERNAME }}/backstage-server:latest"
        
        # Pull and run the server image from Docker Hub
        docker run -d -p 3000:3000 \
          -e DATABASE_HOST=localhost \
          -e DATABASE_USER=testuser \
          -e DATABASE_PASSWORD=testpass \
          -e DATABASE_NAME=testdb \
          -e ACCESS_TOKEN_SECRET=test_secret \
          --name backstage-server-test \
          ${{ secrets.DOCKER_USERNAME }}/backstage-server:latest
        
        # Wait for container to start
        sleep 10
        
        # Test if container is running
        docker ps | grep backstage-server-test || exit 1
        
        # Cleanup
        docker stop backstage-server-test
        docker rm backstage-server-test

    - name: Test Auth Server Docker image from Docker Hub
      run: |
        echo "Testing Auth Server image: ${{ secrets.DOCKER_USERNAME }}/backstage-auth:latest"
        
        # Pull and run the auth server image from Docker Hub
        docker run -d -p 4000:4000 \
          -e DATABASE_HOST=localhost \
          -e DATABASE_USER=testuser \
          -e DATABASE_PASSWORD=testpass \
          -e DATABASE_NAME=testdb \
          -e ACCESS_TOKEN_SECRET=test_secret \
          -e REFRESH_TOKEN_SECRET=test_refresh_secret \
          --name backstage-auth-test \
          ${{ secrets.DOCKER_USERNAME }}/backstage-auth:latest
        
        # Wait for container to start
        sleep 10
        
        # Test if container is running
        docker ps | grep backstage-auth-test || exit 1
        
        # Cleanup
        docker stop backstage-auth-test
        docker rm backstage-auth-test

  deploy:
    runs-on: ubuntu-latest
    needs: [build-and-push-server, build-and-push-auth, test-docker-images]
    if: github.ref == 'refs/heads/main'
    environment: production
    
    steps:
    - name: Deploy to production
      run: |
        echo "Ready for production deployment"
        echo "Images available on Docker Hub:"
        echo "📦 Server: ${{ secrets.DOCKER_USERNAME }}/backstage-server:latest"
        echo "📦 Auth Server: ${{ secrets.DOCKER_USERNAME }}/backstage-auth:latest"
        echo ""
        echo "Set these secrets in GitHub for automated deployment:"
        echo "- PRODUCTION_SERVER_HOST"
        echo "- PRODUCTION_SERVER_USER" 
        echo "- PRODUCTION_SERVER_KEY"
        echo "- DATABASE_USER (override default)"
        echo "- DATABASE_PASSWORD (override default)"
        echo "- DATABASE_HOST (production database)"
        
        # Uncomment when secrets are configured:
        # - name: Deploy via SSH
        #   uses: appleboy/ssh-action@v1.0.0
        #   with:
        #     host: ${{ secrets.PRODUCTION_SERVER_HOST }}
        #     username: ${{ secrets.PRODUCTION_SERVER_USER }}
        #     key: ${{ secrets.PRODUCTION_SERVER_KEY }}
        #     script: |
        #       cd /path/to/your/app
        #       docker compose pull
        #       docker compose up -d