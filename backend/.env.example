# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=your_database_username_here
DATABASE_PASSWORD=generate_secure_password_with_openssl_rand
DATABASE_NAME=backstage

# JWT Authentication - GENERATE STRONG SECRETS!
# Use: openssl rand -hex 64
ACCESS_TOKEN_SECRET=generate_strong_jwt_secret_with_openssl_rand
REFRESH_TOKEN_SECRET=generate_strong_refresh_secret_with_openssl_rand

# Node Environment
NODE_ENV=development

# Security Notice:
# NEVER use these example values in production!
# Generate secure credentials with: openssl rand -hex 32